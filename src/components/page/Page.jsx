import CardFeatureSection from "../card-feature-section/CardFeatureSection";
import CardFeatureWrapper from "../wrapper/CardFeatureWrapper";

const Page = () => (
  <CardFeatureWrapper>
    <CardFeatureSection />
  </CardFeatureWrapper>
);
export default Page;
